<UserControl x:Class="POSDesktopSystem.Presentation.Views.Products.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0 0 0 20">
            <StackPanel>
                <TextBlock Text="Inventory Management"
                           Style="{DynamicResource Heading1}"
                           Foreground="{DynamicResource AccentColor}"/>
                <TextBlock Text="{Binding TotalCount, StringFormat='Managing {0} products in stock'}"
                           Style="{DynamicResource BodyText}"
                           Foreground="{DynamicResource SecondaryTextColor}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <TextBox Width="250"
                         Style="{DynamicResource InputTextBoxStyle}"
                         Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Search products..."
                         Margin="0 0 10 0"
                         Height="42"/>
                <Button Content="+ ADD NEW PRODUCT" 
                        Width="200"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Command="{Binding AddCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Main Content Card -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="12" 
                BorderThickness="1" 
                BorderBrush="{DynamicResource BorderColor}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0"
                          Style="{DynamicResource BaseDataGridStyle}"
                          ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="BARCODE" Binding="{Binding Barcode}" Width="150" FontWeight="Bold"/>
                        <DataGridTextColumn Header="PRODUCT NAME" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="150"/>
                        <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat='{}{0:N2}'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Padding" Value="0 0 15 0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="STOCK" Binding="{Binding StockQuantity}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                                            <Setter Property="Foreground" Value="{DynamicResource ErrorColor}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="STATUS" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8 4" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontWeight="Bold" FontSize="11">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="ACTIVE"/>
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Text" Value="INACTIVE"/>
                                                            <Setter Property="Foreground" Value="#C62828"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="EDIT" 
                                                Padding="10 4" Margin="0 0 5 0"
                                                Style="{DynamicResource SecondaryButtonStyle}"
                                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="DEL" 
                                                Padding="10 4"
                                                Style="{DynamicResource DangerButtonStyle}"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Footer / Pagination -->
                <Border Grid.Row="1" Padding="20 15" BorderThickness="0 1 0 0" BorderBrush="{DynamicResource BorderColor}" Background="#FAF9F6">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="PREVIOUS" 
                                    Width="100"
                                    Style="{DynamicResource SecondaryButtonStyle}"
                                    Command="{Binding PreviousPageCommand}"
                                    IsEnabled="{Binding CurrentPage, Converter={StaticResource InverseBoolToVis}, ConverterParameter=1}"/>
                            <TextBlock Text="{Binding CurrentPage, StringFormat='PAGE {0}'}" 
                                       VerticalAlignment="Center" 
                                       Margin="30 0" 
                                       FontWeight="Bold" 
                                       Foreground="{DynamicResource SecondaryTextColor}"/>
                            <Button Content="NEXT" 
                                    Width="100"
                                    Style="{DynamicResource SecondaryButtonStyle}"
                                    Command="{Binding NextPageCommand}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

<Window x:Class="POSDesktopSystem.Presentation.Views.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:POSDesktopSystem.Presentation.Converters"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="POS Desktop System" 
        WindowState="Maximized"
        Background="{DynamicResource PrimaryBackgroundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Sidebar -->
        <Border Grid.Column="0"
                Background="{DynamicResource SecondaryBackgroundColor}"
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="0 0 1 0">
            <DockPanel>
                <!-- App Brand -->
                <StackPanel DockPanel.Dock="Top" Margin="0 0 0 20">
                    <Border Background="{DynamicResource AccentColor}" Padding="20 30" CornerRadius="0 0 20 0">
                        <StackPanel>
                            <TextBlock Text="Smart Inventory"
                                       Foreground="White"
                                       FontSize="20"
                                       FontWeight="Black"/>
                            <TextBlock Text="POS SYSTEM"
                                       Foreground="White"
                                       Opacity="0.7"
                                       FontSize="11"
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Margin="20 20 20 0" Background="{DynamicResource TertiaryBackgroundColor}" CornerRadius="8" Padding="12">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="32" Height="32" CornerRadius="16" Background="{DynamicResource AccentColor}">
                                <TextBlock Text="👤" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14"/>
                            </Border>
                            <StackPanel Margin="12 0 0 0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding CurrentUsername}"
                                           Foreground="{DynamicResource PrimaryTextColor}"
                                           FontSize="13"
                                           FontWeight="Bold"/>
                                <TextBlock Text="Online"
                                           Foreground="#2E7D32"
                                           FontSize="10"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Logout at bottom -->
                <StackPanel DockPanel.Dock="Bottom" Margin="15 0 15 20">
                    <Button Content="LOGOUT"
                            Height="45"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Command="{Binding LogoutCommand}"/>
                </StackPanel>

                <!-- Navigation Links -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10 0">
                        <TextBlock Text="MAIN MENU" 
                                   Foreground="{DynamicResource MutedTextColor}" 
                                   FontSize="10" 
                                   FontWeight="Bold" 
                                   Margin="15 0 0 10"/>
                        
                        <RadioButton Style="{DynamicResource NavRadioButtonStyle}"
                                     Content="Point of Sale"
                                     IsChecked="True"
                                     Command="{Binding NavigateToPosCommand}"/>
                        
                        <RadioButton Style="{DynamicResource NavRadioButtonStyle}"
                                     Content="Products"
                                     Command="{Binding NavigateToProductsCommand}"
                                     Visibility="{Binding IsManagerRole, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        
                        <RadioButton Style="{DynamicResource NavRadioButtonStyle}"
                                     Content="Invoice History"
                                     Command="{Binding NavigateToInvoicesCommand}"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header Bar -->
            <Border Grid.Row="0" Background="White" Height="60" BorderThickness="0 0 0 1" BorderBrush="{DynamicResource BorderColor}">
                <Grid Margin="20 0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentViewTitle}" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="{DynamicResource PrimaryTextColor}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat='{}{0:dddd, dd MMMM yyyy}'}" 
                                   Foreground="{DynamicResource SecondaryTextColor}" 
                                   FontSize="13"
                                   Margin="0 0 20 0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content View -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
        </Grid>
    </Grid>
</Window>
